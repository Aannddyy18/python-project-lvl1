name: make-lint
on: push
jobs:
  my-job:
    runs-on: macos-latest
    steps:
      - name: Install dependencies
        run: |
          pip install poetry
          make install
      - name: my-step
        run: make lint
